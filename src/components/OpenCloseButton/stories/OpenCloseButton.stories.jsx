import React from 'react';
import OpenCloseButton from '../OpenCloseButton';

export default {
  title: 'OpenCloseButton',
  component: OpenCloseButton,
}

export const Default = () => <OpenCloseButton isOrphan={true}/>